const numbers = [2, 6, 1, 5, -1, 4, 9]; // finding the max between an array with math.max() method ..



const maxfind = Math.min(...numbers)
console.log(maxfind);



// const max =  numbers.sort((a, b )=>a-b)
// console.log(max); // we can also find this using sort() method 



//  numbers.reduce((acc, acctualval)=>{

// if(acctualval<numbers){
// }

// return acc

// }, 0)